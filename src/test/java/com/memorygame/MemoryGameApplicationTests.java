package com.memorygame;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test — verifies that the Spring application context loads
 * without errors. Run with: mvn test
 */
@SpringBootTest
class MemoryGameApplicationTests {

    @Test
    void contextLoads() {
        // If the application context starts up without throwing an exception, this test passes.
    }
}
